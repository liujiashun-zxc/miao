var liujiashun_zxc = {
  chunk: function(array, size) {
  
  },
  compact: function(array) {
    
  },
  concat : function (arr,arg){
  let result = arr
    for (let item of arg) {
      if (Array.isArray(item)) {
        result.push(subitem)
      }
      else {
        retult.push(item)
      }
      return retult
    }

}
